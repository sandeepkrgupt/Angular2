import { FontCngHoverDirective } from './font-cng-hover.directive';

describe('FontCngHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new FontCngHoverDirective();
    expect(directive).toBeTruthy();
  });
});
