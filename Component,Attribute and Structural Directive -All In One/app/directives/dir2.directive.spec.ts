import { Dir2Directive } from './dir2.directive';

describe('Dir2Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir2Directive();
    expect(directive).toBeTruthy();
  });
});
