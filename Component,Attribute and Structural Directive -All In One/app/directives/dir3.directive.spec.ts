import { Dir3Directive } from './dir3.directive';

describe('Dir3Directive', () => {
  it('should create an instance', () => {
    const directive = new Dir3Directive();
    expect(directive).toBeTruthy();
  });
});
