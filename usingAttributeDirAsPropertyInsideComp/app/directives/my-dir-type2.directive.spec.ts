import { MyDirType2Directive } from './my-dir-type2.directive';

describe('MyDirType2Directive', () => {
  it('should create an instance', () => {
    const directive = new MyDirType2Directive();
    expect(directive).toBeTruthy();
  });
});
