import { LoopDirDirective } from './loop-dir.directive';

describe('LoopDirDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopDirDirective();
    expect(directive).toBeTruthy();
  });
});
